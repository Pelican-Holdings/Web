export interface Product {
  id: number;
  name: string;
  price: number;
  image: string;
  description: string;
}

export interface CompanyValue {
  id: number;
  name: string;
  icon: string;
  description: string;
}
